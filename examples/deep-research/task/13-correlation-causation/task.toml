schema_version = "1.4"
artifacts = ["/app/research-report.md", "/app/research-result.json"]

[task]
name = "concepts/13-correlation-causation"
version = "4.0.0"
description = "Chinese concept explanation with grounded retrieval and ternary evaluation."
authors = []
keywords = ["deep-research", "concept-explanation", "citations"]

[metadata]
query = "两个现象总是一起出现，就能说明一个导致了另一个吗？"
topic = "相关与因果"
case_type = "causal-reasoning"
badcase = true

[verifier]
timeout_sec = 120.0
collect = []

[verifier.env]

[agent]
timeout_sec = 1200.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
