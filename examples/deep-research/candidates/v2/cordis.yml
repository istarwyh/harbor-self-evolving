- id: candidate-llm
  name: './research-agent.mjs'

- id: acp-agent
  name: '@deepseek-ai/dsh-acp-demo'
  config:
    provider: harbor-demo
    model: gpt-5.6-luna
    persistenceRoot: /tmp/dsh-sessions
    persistenceCompression: none
    workspaceContext: false
    persona: |
      You are a business research Agent under evaluation. Complete the user task through the configured generator.
