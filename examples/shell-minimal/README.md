# Shell minimal

不依赖 DeepSeek Harness 的最小 Candidate 示例，用来理解 Candidate、Job、Trial 和 Gate：

```bash
HARBOR_BIN=/absolute/path/to/harbor ./scripts/run-evolution-demo.sh
```

v1 写出错误文本，v2 修复它；两个 Job 的证据写入仓库根目录的 `jobs/`。
