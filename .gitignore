.DS_Store
__pycache__/
*.py[cod]

# Harbor execution results are local evidence, not template source.
jobs/*
!jobs/.gitkeep

# Local Docker/Buildx state; keep only the portable config file.
.runtime/docker/buildx/
.runtime/docker/contexts/
